# TODO / 향후 작업

Easy Fulfill 추후 작업 백로그. 완료 시 항목을 지우지 말고 `- [x]` 로 체크하고 완료일 표기.

## API · 알림 통합 (진행 중)

- [x] 미답변 문의 리마인더 모델로 전환(네이버) — 2026-06-26
      → `docs/inquiry-alerts-naver-coupang.md`
- [x] 쿠팡 미답변 문의 통합(온라인·콜센터, HMAC 인증) — 2026-06-26
- [ ] **쿠팡 배송/주문 API 연동** (다음 단계)
      - 발주서 조회 `GET …/ordersheets` (status: INSTRUCT/DEPARTURE/FINAL_DELIVERY 등)
      - 배송상태 변경 이력 조회
      - 목적: 배송 모니터링을 쿠팡까지 확장(현재 우체국 종추적 위주)
- [ ] (검토) 다중 PC **시트 클레임 락** — 브랜드뉴 문의 동시 도착 시 중복 발송 완전 차단.
      현재는 60분 리마인더 창 + PC별 가동시각 차로 자연 분산(실사용상 드묾). 필요 시 도입.
- [ ] (검토) 「문의알림」 시트 **링버퍼/정리** — 누적 행 증가 대응. 피드백 후.

## 리팩터 (기능 추가 전에 권장)

- [ ] **모니터링 워커 모듈 추출** — `easy-fulfill.py`(7천 줄+)에서 폴링 워커/스레드를
      `monitoring.py`(가칭)로 이동. UI 안 건드리는 순수 함수 이동이라 리스크 작음.
      대상: `run_naver_inquiry_poll_worker`(네이버+쿠팡), 배송 위험/다이제스트 워커, 관련 Thread 클래스.
      → **다음 API 기능을 붙이기 전에** 하는 것을 추천(메인 파일 비대화 방지).
- [ ] API provider 로직은 계속 **provider별 파일 분리** 유지(`*_commerce.py` / `*_tracker.py`).
      이미 적용 중: `naver_commerce.py`, `coupang_commerce.py`, `kpost_tracker.py`, `slack_notify.py`.

## UI 구조

- [ ] **배송추적 탭 → "모니터링/알림센터" 탭 분리** — 지금은 보류(YAGNI). 아래 트리거 중
      하나라도 오면 진행:
      1. 3번째 독립 API 도메인 추가(예: 정산/재고처럼 배송·문의와 결이 다른 것)
      2. 설정 팝업이 스크롤 없이 안 보일 만큼 그룹·버튼이 늘어남
      3. 알림 채널을 "알림센터(AlertHub)"로 브랜딩 통일 확정
      → 그때 탭 개명 + 그룹박스(배송/문의/정산…)로 분할이 가장 매끄러움.

## 네이밍 (보류)

- [ ] 통합 알림 봇/채널 이름 확정 — 후보 **AlertHub**(1순위), Ops Center, AlertCenter.
      앱 이름까지 통일할지 별도 결정. 현재 앱 타이틀 "Easy Fulfill - 주문 처리 시스템" 유지.
      슬랙 채널명만 바꾸는 건 코드 무관(슬랙에서 변경). 앱/봇 표기 통일 시 코드 문구 수정 필요.
